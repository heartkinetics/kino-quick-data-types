{
  "external-reference": {
    "description": "To reference a user in an external app/service.",
    "formats": {
      "kino-research": {
        "description": "Reference in kino-research app",
        "type": "object",
        "properties": {
          "studyCode": {
            "description": "The code of the study",
            "type": "string"
          },
          "subjectCode": {
            "description": "The code of the subject",
            "type": "string"
          }
        },
        "required": [
          "studyCode",
          "subjectCode"
        ]
      },
      "firebase-cloud-messaging": {
        "description": "FCM token",
        "type": "object",
        "properties": {
          "token": {
            "description": "Token",
            "type": "string"
          }
        },
        "required": [
          "token"
        ]
      }
    }
  }
}